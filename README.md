# Bible Verse Website

A simple, elegant website that displays random Bible verses with themed background images.

## Features

- Random Bible verse display
- Themed background images
- Mobile-responsive design
- Smooth transitions
- One-click verse refresh

## Project Structure

```text
verse-static/
├── .cursor/              # Cursor AI configuration and rules
│   └── rules/
├── dist/                 # Build output directory
│   └── images/           # Processed images for production
├── files/                # Source files for data preparation
│   └── whole_bible_niv1984.pdf # Source Bible text
├── public/               # Static assets served directly or processed
│   ├── index.html        # Main webpage template
│   ├── styles.css        # Base CSS styles (processed by PostCSS)
│   ├── script.js         # Frontend logic (processed by esbuild)
│   └── images/           # Source background images
├── scripts/              # Build, development, and utility scripts
│   └── modules/          # Potential location for script modules
├── src/                  # Source files (if any, structure may vary)
├── tasks/                # Task management files (generated by Taskmaster)
│   └── tasks.json        # Main task list
├── .DS_Store             # macOS directory metadata (ignored by git)
├── .env                  # Environment variable configuration (required, see below)
├── .gitignore            # Specifies intentionally untracked files
├── .windsurfrules        # Configuration for Windsurf (likely related to Cursor)
├── count.js              # Unclear purpose, potentially utility script
├── esbuild-meta.json     # Metadata output from esbuild builds
├── LICENSE               # Project license information
├── package-lock.json     # Exact dependency versions
├── package.json          # Project dependencies and scripts
├── postcss.config.js     # Configuration for PostCSS processor
├── README-task-master.md # Detailed guide for the Taskmaster workflow
└── README.md             # This file
```

## Setup

1. **Environment:** Copy `.env.example` to `.env` (if it exists) or create a `.env` file.
    Configure necessary variables (see `.env` section or code).
2. **Install Dependencies:**

    ```bash
    npm install
    ```

3. **Prepare Data:** (If required for initial setup or data changes)
    - Process `files/whole_bible_niv1984.pdf`, possibly fetch images.
        Check `package.json` for scripts (e.g., `prepare`, `extract-verses`).
        Run the appropriate script(s).
4. **Build Assets:**

    ```bash
    npm run build # Or the relevant build script from package.json
    ```

5. **Start the Server:**

    ```bash
    npm start
    ```

6. Visit `http://localhost:3000` (or the configured port) in your browser.

## Scripts (Common examples - check `package.json` for specifics)

- `npm run build`: Builds assets (CSS, JS) to `dist/` using `esbuild` and `postcss`.
- `npm start`: Starts the development or production server (likely Node.js/Express).
- `npm run dev`: Starts dev server, potentially with hot-reloading/watch mode.
- `npm run prepare`/`extract-verses`/etc.: Data preparation scripts (see Setup step 3).
- `npm run lint`/`format`: Code linting and formatting commands.

## Development Workflow

This project uses **Taskmaster** for managing development tasks.

Please refer to [README-task-master.md](mdc:README-task-master.md) for the detailed workflow
using the `task-master` CLI tool (or `node scripts/dev.js`).

The general process involves:

1. Listing tasks (`task-master list`).
2. Viewing task details (`task-master show <id>`).
3. Implementing features based on task details.
4. Marking tasks complete (`task-master set-status --id=<id> --status=done`).

## Technology Stack

- **Backend:** Node.js with Express.js (likely).
- **Frontend:** HTML, CSS, JavaScript.
- **Build Tools:** esbuild (JS bundling), PostCSS (CSS processing).
- **Data Preparation:** PDF parsing script (e.g., `src/extract-verses.js`).
- **Task Management:** Taskmaster ([README-task-master.md](mdc:README-task-master.md)).
- **Images:** Sourced potentially from CDN/local files, processed to `dist/images`.

## Environment Variables (`.env`)

A `.env` file is required at the root for configuration. Common variables might include:

- `PORT`: Server port (e.g., `PORT=3000`)
- API keys or URLs if external services are used.

(Consult application code or setup instructions for a complete list)

## Contributing

1. Fork the repository.
2. Create your feature branch.
3. Commit your changes.
4. Push to the branch.
5. Create a Pull Request.

## License

This project is licensed under the MIT License - see the [LICENSE](mdc:LICENSE) file for details.
